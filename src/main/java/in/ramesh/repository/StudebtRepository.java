package in.ramesh.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import in.ramesh.entity.StudentEntity;

public interface StudebtRepository extends JpaRepository<StudentEntity	,Integer>{

}
